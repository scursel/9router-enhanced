# Accepted failures. A failure NOT listed here is a regression.
#
# EMPTY as of 2026-09-21: the 39 catalogued reds were fixed.
#   - 35 in unit/cursor-agent-proto.test.js: the AgentService (agent.v1) codecs
#     now exist — encodeAgentValue/decodeAgentValue (google.protobuf.Value),
#     encodeMcpToolDefinition, encodeMcpTools, decodeMcpArgs,
#     encodeMcpResultSuccess/Error/ToolNotFound in
#     open-sse/utils/cursorProtobuf.js, plus exported isAgentCapableRequest and
#     a 3-arg buildAgentRunFrame in open-sse/executors/cursor.js. Field numbers
#     verified against Cursor's agent.proto via can1357/oh-my-pi @ 60c9a115.
#     Routing still gates on isAgentTextRequest, so live wire behavior is
#     unchanged.
#   - 4 in the Command Code suites (unit/openai-to-commandcode,
#     unit/commandcode-to-openai, translator/bugs-gemini-cursor-commandcode):
#     assertions updated to the current translator behavior from upstream
#     13b468b8/092c84ea — image blocks carry both mimeType and mediaType, and
#     mid-stream errors throw a readable Error instead of emitting a chunk.
#   - unit/embeddings.cloud.test.js no longer fails to COLLECT: the cloud/
#     worker sources are not part of this repo, so the suite self-skips via a
#     guarded dynamic import until that directory lands.
#
# Any test failure in the offline run is now a regression by definition.
