# Keep a single React copy: components under src/ resolve react from the repo
# root node_modules; an auto-installed peer here would create a second copy and
# break hooks ("invalid hook call") when tests render those components.
legacy-peer-deps=true
